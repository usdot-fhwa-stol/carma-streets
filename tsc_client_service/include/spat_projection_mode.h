#pragma once

#include <stdlib.h>

namespace traffic_signal_controller_service{
    enum class spat_projection_mode {
        no_projection = 0,
        dpp_projection = 1,
        fixed_projection =2
        
    };
}